;(function () {
  const swiper = new Swiper('.swiperFeaturedHouse', {
    slidesPerView: '1',
    spaceBetween: 30,
  })

  const heroSectionSwiper = new Swiper('.swiper-hero-section', {
    slidesPerView: 'auto',
    spaceBetween: 16,
    freeMode: true,
  })
})()
